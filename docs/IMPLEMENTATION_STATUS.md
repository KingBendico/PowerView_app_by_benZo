The `codex/powerview-desktop` branch integrates the current local PowerView app supplied on 16 September 2026, reliability improvements and the redesigned desktop interface.

The original source folder and its uncommitted changes remain untouched. A baseline snapshot and original-review evidence are kept in the parent workspace's evaluation directory. The GitHub integration includes the corrected assessment, current features, implementation, tests and validation record.

Implemented: shared gateway state, restricted Electron bridge, finite requests and cancellation, per-home preferences and legacy migration, capability-aware control, consistent closed percentages, group failure results, immediate Stop, Home/pinned shades, isolated demo, direct tray scene execution, local icons, persistent navigation and modal/rail keyboard improvements. The redesign adds a desktop sidebar, warm neutral and forest themes, compact textured shade illustrations, whole-home Home actions, room shortcuts, prioritized pinned controls and keyboard search across rooms, shades and scenes. Narrow windows use bottom navigation.

The 17 September update separates draggable targets from the movement graphic, guards against stale refreshes and late acknowledgments, and animates travel from gateway motion events when a travel time is supplied. Estimated progress is labeled; final reports settle the position. Demo mode now simulates travel, retargeting and intermediate Stop. A per-device appearance editor adds shades/blinds or curtains, fabric textures, preset/custom colors, preview and reset, persisted separately for each home. Curtains can use a center-opening pair or a single-draw panel opening to the left or right. Real two-rail devices retain both rail controls and offer fabric/color choices; demo two-rail devices can switch to curtain controls and back. See MOTION_AND_APPEARANCE.md for behavior and limits. Validation passes 34 core tests and 23 native Electron checks.

Retained: visual shade dragging and dual grips, Jog, fine adjustment, scenes, favorites, recents, room sorting, room color indicators/icons, theme switching, connection freshness, live events, manual setup, discovery, Swagger tools and macOS build configuration. The supplied screenshot's large white panel and glossy blue graphics have been replaced with the new layout and visual treatment.

Custom global shortcuts now cover individual shades (including arbitrary closed percentages), rooms, the whole home, gateway scenes, show/hide and refresh. The Settings editor includes key recording, optional starters, per-binding and per-home enable controls, conflict feedback, atomic save validation and per-home/demo persistence. Editing pauses registrations; window hide/close and renderer reload/crash restore the saved configuration. Stale callbacks and offline movement are discarded. Validation now passes 46 core tests and 29 native Electron checks; physical OS keyboard delivery and hardware movement remain manual checks. See SHORTCUTS.md.

Remaining release validation is documented in VALIDATION.md. Schedules, multi-home profiles, scene previews and glare automation remain roadmap ideas, not implemented features. NEXT_IDEAS.md prioritizes temporary privacy timers, named positions/custom groups, activity history and a device health overview.
